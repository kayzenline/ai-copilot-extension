{
  "name": "ai-copilot-extension",
  "version": "1.0.0",
  "description": "Chrome AI Copilot Extension — Advanced TypeScript Architecture",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "tsc && vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^22.0.0",
    "fs-extra": "^11.4.1",
    "jsdom": "^26.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
